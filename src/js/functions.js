// Functions
////////////

//Alert Log Some Message Message
var notify = (message) => alert(message)

//Console Log Some Message Message
var log = (message) => console.log(message)

export default{
    log,
    notify
}